# cv2.rectangle(img_to_show, (x, y), (x + w, y + h), (0, 255, 0), 2)
import cv2


def draw_rects(img, rects, color=(255,0,0)):
    for rect in rects:
        # cv2.rectangle(img, )
        pass
    return img